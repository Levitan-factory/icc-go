# Demo Assets

`icc-go-60s-demo.gif` is generated by `npm run demo:gif`.

The demo is intentionally deterministic so the public README can be rebuilt during release preparation without relying on a hosted notebook session.
