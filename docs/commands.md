# ICC Command Reference

The command reference moved into the structured documentation set:

- `reference/language.md`: routing, constraints, flow commands, outputs, references, and statuses.
- `reference/workspace.md`: menus, shortcuts, exports, and workspace controls.
- `examples/`: copyable workflow patterns.

Open the in-app documentation from `Help -> ICC DSL Wiki`.
